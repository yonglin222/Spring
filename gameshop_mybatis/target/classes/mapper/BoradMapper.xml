<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dw.gameshop_mybatis.mapper.BoardMapper">
    <resultMap id="BoardWithUserMap" type="Board">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createdDate" column="created_date"/>
        <result property="modifiedDate" column="modified_date"/>
        <result property="isActive" column="is_active"/>

        <association property="user" javaType="User">
            <id property="userName" column="user_name"/>
            <result property="email" column="email"/>
            <result property="realName" column="real_name"/>
            <result property="createdAt" column="created_at"/>
            <association property="authority" javaType="Authority">
                <id property="authorityName" column="authority_name"/>
            </association>
        </association>
    </resultMap>
    <select id="getBoardByid" resultMap="BoardWithUserMap">
        select
        b.id, b.title, b.content, b.created_date, b.modified_date,
        b.is_active, u.user_name, u.email, u.real_name, u.created_at,
        a.authority_name
        from board b
        join user u on b.user_name = u.user_name
        join authority a on u.user_authority = a.authority_name
        where b.id = #{id} and b.is_active = true

    </select>
    <select id="getAllBoards" resultMap="BoardWithUserMap">
        select
        b.id, b.title, b.content, b.created_date, b.modified_date,
        b.is_active, u.user_name, u.email, u.real_name, u.created_at,
        a.authority_name
        from board b
        join user u on b.user_name = u.user_name
        join authority a on u.user_authority = a.authority_name
        where b.is_active = true
    </select>
    <insert id="saveBoard" useGeneratedKeys="true" keyProperty="id">
        insert into board (
        title,
        content,
        user_name,
        created_date,
        modified_date,
        is_active
        ) values (
        #{board.title},
        #{board.content},
        #{board.user.userName},
        #{board.createdDate},
        #{board.modifiedDate},
        #{board.isActive}
        )
    </insert>
    <update id="updateBoard">
        update board
        set
        title = #{board.title},
        content = #{board.content},
        modified_date = #{board.modifiedDate}
        where
        id = #{board.id} and is_active = true
    </update>
    <delete id="deleteBoard">
        update board
        set is_active = false
        where id = #{id}
    </delete>
</mapper>