<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dw.gameshop_mybatis.mapper.GameMapper">
    <select id="getAllGames" resultType="Game">
        select id, title, genre, price, image_url, text
        from games
    </select>

    <select id="getGameById" resultType="Game">
        select id, title, genre, price, image_url, text
        from games
        where id = #{id}
    </select>
</mapper>